apiVersion: v1
kind: Service
metadata:
  name: thanos-mcp
  namespace: <your-namespace>
spec:
  type: NodePort
  selector:
    app: thanos-mcp
  ports:
  - port: 8080
    targetPort: 8080
    protocol: TCP
